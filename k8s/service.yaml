apiVersion: v1
kind: Service
metadata:
  name: demo-app-web-service
spec:
  type: NodePort
  selector:
    app: demo-app-web
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30500
